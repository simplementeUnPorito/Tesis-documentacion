\section{Self-Calibrating Geophone Platform}
\label{sec:self-calibration}

\subsection{Digital Hardware and Calibration Sequence}

% Editable diagram source: imagenes/psoc_foreground_calibration_flow.drawio
% After tuning it in diagrams.net, export it as
% imagenes/psoc_foreground_calibration_flow.pdf and replace this draft box
\begin{figure}[!ht]
	\centering
	\includegraphics[width=0.68\columnwidth]{imagenes/psoc_foreground_calibration_flow.drawio.png}
	\caption{Flowchart of the proposed foreground autocalibration algorithm, including stored-code verification, sequential per-stage correction, and transition to acquisition.}
	\label{fig:calibration_flow}
\end{figure}

At boot, after a gain change, or on command, the firmware loads stored DAC codes and verifies the resulting offsets (Fig.~\ref{fig:calibration_flow}). If all stages are within tolerance, it restores acquisition without a full run; otherwise it processes PGA, BP, SUM, and LP sequentially, resetting the FIR history and settling after each multiplexer switch. A watchdog and per-stage sample limit bound the sequence. During acquisition the controller is inactive, the multiplexer selects LP, and the VDAC codes remain constant, so no time-varying feedback enters the acquired signal.

\begin{figure*}[!t]
	\centering
	\includegraphics[width=0.80\textwidth]{imagenes/DigitalPathGordito.png}
	\caption{Digital calibration hardware. The controller routes DMA transfers among the ADC, shared digital filter, and EEPROM and reconfigures the analog multiplexer. Black and blue arrows indicate acquisition and calibration paths, respectively.}
	\label{fig:digital_path}
\end{figure*}

The shared digital filter implements the geophone response during acquisition and switches to an aggressive low-pass estimator during calibration (Fig.~\ref{fig:digital_path}); its acquisition coefficients and path are then restored. Successful DAC codes are stored in EEPROM as one record per PGA gain setting containing the gain code, valid-stage mask, four DAC codes, version, and CRC. A failed run never overwrites a valid record.

\subsection{Control Law and Calibration Parameters}

For each stage, the filtered measurement $\hat{y}_i[n]$ produces the ADC-domain error $e_i[n] = r_i-\hat{y}_i[n]$, where $r_i=0$ for the current differential geophone configuration. The error is first mapped to the 8-bit DAC domain:
\begin{equation}
 \varepsilon_i[n] = \operatorname{round}\!\left(
 \frac{e_i[n]V_{\mathrm{ADC}}N_{\mathrm{DAC}}}
 {N_{\mathrm{ADC}}V_{\mathrm{DAC}}}\right),
 \label{eq:scale}
\end{equation}
with $V_{\mathrm{ADC}}=5000$~mV, $N_{\mathrm{ADC}}=262144$, $V_{\mathrm{DAC}}=4080$~mV, and $N_{\mathrm{DAC}}=255$ in the implemented configuration.

The rounding in Eq.~\ref{eq:scale} makes $\varepsilon_i[n]$ an integer error in equivalent DAC codes. If the ideal operating point lies between two realizable codes, an acceptance band narrower than one output step can make the controller alternate indefinitely between them. The implemented deadband therefore scales with the DAC-to-node gain,
\par\noindent
\begin{minipage}[c]{0.45\columnwidth}
\footnotesize
\begin{equation}
 \Delta_i=\max(1,\lceil1.2|K_i|\rceil),
 \label{eq:deadband}
\end{equation}
\end{minipage}%
\hfill
\begin{minipage}[c]{0.54\columnwidth}
\footnotesize
\begin{equation}
 \bar{\varepsilon}_i[n]=
 \begin{cases}
 0, & |\varepsilon_i[n]|\leq\Delta_i,\\
 \varepsilon_i[n], & \text{otherwise},
 \end{cases}
 \label{eq:deadband-error}
\end{equation}
\end{minipage}
\par
where $\bar{\varepsilon}_i[n]$ is the error presented to the controller. The 1.2 factor provides a small margin over one realizable stage-output step, while the one-code floor preserves a finite acceptance region when $|K_i|<1$.

The architecture requires only a measurable stage output and sufficient trim range; this prototype uses a positional PI law:
\begin{equation}
 u_i^*[n]=u_{i,0}+s_i\left[
 \frac{k_{p,i}}{|K_i|}\bar{\varepsilon}_i[n]+\frac{k_{i,i}}{|K_i|}
 \sum_{q=0}^{n}\bar{\varepsilon}_i[q]\right],
 \label{eq:pi}
\end{equation}
Here $u_{i,0}$ is a feedforward code near the expected operating point, $s_i\in\{-1,+1\}$ is the injection polarity, and $|K_i|$ is the nominal stage movement per VDAC code in the scaled domain of Eq.~\ref{eq:scale}. The fixed BP, SUM, and LP values are $2$, $7.89$, and $6$, respectively; PGA $K_i$ and $s_i$ follow the active gain code. The feedforward codes were hand-set once on Board~B at the power-up gain $\times50$ and then used unchanged for Board~A and all tested gains. With $k_{p,i}=0$ for all stages, Eq.~\ref{eq:pi} reduces to the pure integrator parameterized by $k_{i,i}$ in Table~\ref{tab:cal_params}.

The command is clamped to the VDAC range and slew-limited to one code per sample, with conditional integration for anti-windup. Convergence requires a fixed number of samples in the same quantized bucket; a final adjacent-code trial is retained only when it lowers the filtered error. At timeout, the best code found is held, the stage is flagged not converged, and the sequence advances.

Table~\ref{tab:cal_params} lists the implemented parameters. Settling uses 512 samples (0.20~s) for PGA/BP/SUM and 1024 (0.39~s) for LP; lock requires 512 or 1024 consecutive in-deadband samples, and refinement adds 1024--2048 samples. The listed timeouts are worst-case ceilings.

\begin{table}[t]
	\caption{Implemented per-stage calibration parameters. Deadband $\Delta_i$ is given in equivalent DAC codes; the DC estimator runs at the ADC native sample rate of 2604~Hz.}
	\label{tab:cal_params}
	\centering
	\scriptsize
	\setlength{\tabcolsep}{2.5pt}
	\resizebox{0.98\columnwidth}{!}{%
		\begin{tabular}{lcccc@{\hspace{4pt}\vrule\hspace{4pt}}lcccc}
			\toprule
			Stage & $K_i$ & $\Delta_i$ & $k_{i,i}$ & Timeout (s)
			& Stage & $K_i$ & $\Delta_i$ & $k_{i,i}$ & Timeout (s)\\
			\midrule
			PGA   & gain-dependent & gain-dependent & $3\times10^{-4}$ & 11.5
			& SUM & 7.89 & 10 & $5\times10^{-4}$ & 11.5\\

			BP    & 2 & 3 & $5\times10^{-4}$ & 17.3
			& LP    & 6 & 8 & $5\times10^{-4}$ & 17.3\\
			\bottomrule
		\end{tabular}%
	}
\end{table}

The 8-bit VDAC resolution meets this prototype's acceptance range. Applications requiring a smaller residual can trade added hardware for a finer step through a PSoC IDAC/resistor or a buffered topology \cite{InfineonAN60305}, while retaining the same measure--estimate--correct sequence.
