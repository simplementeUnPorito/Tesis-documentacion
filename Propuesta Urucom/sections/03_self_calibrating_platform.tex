\section{Self-Calibrating Geophone Platform}
\label{sec:self-calibration}

\subsection{Digital Hardware and Calibration Sequence}

% Editable paper diagram source: imagenes/psoc_foreground_calibration_flow_compressed.drawio (page 2)
\begin{figure}[!t]
	\centering
	\includegraphics[width=\columnwidth]{imagenes/psoc_foreground_calibration_flow.drawio_horizontal_comprimido.png}
	\caption{Foreground calibration flow: stored-code verification, sequential trimming, and acquisition.}
	\label{fig:calibration_flow}
\end{figure}

At boot, after a gain change, or on command, the firmware loads stored DAC codes and verifies the offsets (Fig.~\ref{fig:calibration_flow}). Because DC estimation assumes a stationary geophone and slow internal-state change over the lock window, an accepted operating point returns directly to acquisition; otherwise PGA, BP, SUM, and LP are processed sequentially, with filter reset and settling after each switch. A watchdog and sample limits bound the sequence; during acquisition the controller is inactive, LP is selected, and the VDAC codes are constant.

The shared digital filter is reconfigured between modes: acquisition loads coefficients tailored to the geophone's operational bandwidth, whereas calibration uses a 128-tap low-pass finite-impulse-response (FIR) to estimate the DC offset, with direct memory access moving data through RAM. After calibration the acquisition coefficients, filter state, and routing are restored. A gain-indexed electrically erasable programmable read-only memory (EEPROM) record stores the valid-stage mask, four DAC codes, version, and cyclic-redundancy check; a failed run may hold its bounded best codes but never overwrites a valid record.

\subsection{Control Law and Calibration Parameters}

For each stage, $\hat{y}_i[n]$ is the filtered signed ADC count proportional to $V_{\mathrm{stage}}-V_{\mathrm{ref}}$, $r_i$ is the desired count, and $e_i[n]$ is the measured residual. Because the target is the reference level, $r_i=0$ and
$e_i[n]=\hat{y}_i[n]-r_i=\hat{y}_i[n]$. The residual is first mapped to the 8-bit DAC domain:
\begin{equation}
 \begin{aligned}
 \varepsilon_i[n]
 &= \operatorname{round}\!\left(e_i[n]\frac{q_{\mathrm{ADC}}}{q_{\mathrm{DAC}}}\right)\\
 &= \operatorname{round}\!\left(
 \frac{e_i[n]V_{\mathrm{ADC,span}}N_{\mathrm{DAC}}}
 {N_{\mathrm{ADC}}V_{\mathrm{DAC,span}}}\right),
 \end{aligned}
 \label{eq:scale}
\end{equation}
where $q_x=V_{x,\mathrm{span}}/N_x$ is the step size and $N_x$ is the number of quantization levels. All quantities used here are nominal datasheet values: $N_{\mathrm{ADC}}=2^{18}$, $N_{\mathrm{DAC}}=2^8$, $V_{\mathrm{ADC,span}}=5000$~mV, and $V_{\mathrm{DAC,span}}=4096$~mV as the ideal 256-bin quantization span, giving $q_{\mathrm{ADC}}=5000/2^{18}$~mV/count and $q_{\mathrm{DAC}}=16$~mV/code \cite{InfineonPSoC,InfineonAN60305}.

The rounding in Eq.~\ref{eq:scale} gives an integer error in equivalent DAC codes. An overly narrow band can alternate indefinitely between adjacent codes when the ideal point is not realizable; the implementation therefore adopts the conservative one-step floor
\par\noindent
\begin{minipage}[c]{0.45\columnwidth}
\footnotesize
\begin{equation}
 \Delta_{i,\min}^{\mathrm{impl}}=\max(1,\lceil|K_i|\rceil),
 \label{eq:deadband}
\end{equation}
\end{minipage}%
\hfill
\begin{minipage}[c]{0.54\columnwidth}
\footnotesize
\begin{equation}
 \bar{\varepsilon}_i[n]=
 \begin{cases}
 0, & |\varepsilon_i[n]|\leq\Delta_i,\\
 \varepsilon_i[n], & \text{otherwise},
 \end{cases}
 \label{eq:deadband-error}
\end{equation}
\end{minipage}
\par
Inside the band, Eq.~\ref{eq:deadband-error} suppresses integration; lock and refinement still govern convergence. Table~\ref{tab:cal_params} lists adopted values satisfying $\Delta_i\geq\Delta_{i,\min}^{\mathrm{impl}}$.

Under the assumptions of Section~\ref{sec:platform-design}, the architecture requires a measurable stage output and sufficient trim authority and range. The controller is written in positional proportional-integral (PI) form,
\begin{equation}
c_i^*[n]=c_{i,\mathrm{base}}-s_i\left[
 \frac{k_{\mathrm{P},i}}{|K_i|}\bar{\varepsilon}_i[n]+\frac{k_{\mathrm{I},i}}{|K_i|}
 \sum_{q=0}^{n-1}\bar{\varepsilon}_i[q]\right],
 \label{eq:pi}
\end{equation}


where $c_{i,\mathrm{base}}$ is the baseline feedforward code and $s_i=\operatorname{sgn}(K_i)$. The integral sum holds samples accepted before the current update and resets when the residual enters the deadband. The VDAC-to-node gain magnitudes $|K_i|$ used in Table~\ref{tab:cal_params} follow from each stage's topology and resistor ratios; at the two-op-amp instrumentation-amplifier PGA input \cite{CypressAN60319}, the reference reaches its measured node at unity DC gain ($K_{\mathrm{PGA}}=1$).


The command is clipped to that stage's configured limits within 0--255; at a limit, integration is suspended when the residual would push the command farther out, preventing windup. Convergence requires a fixed count of consecutive in-deadband samples, and an adjacent-code trial is kept only if it lowers the filtered residual. At timeout the best code is held and the stage flagged.

\begin{table}[t]
\caption{Parameters used for the reported measurements. Deadbands are in equivalent DAC codes; S/L/R are settle/lock/refine samples at 2604~Hz, whereas Timeout is in seconds.}
	\label{tab:cal_params}
	\centering
	\scriptsize
	\setlength{\tabcolsep}{2.0pt}
	\resizebox{0.98\columnwidth}{!}{%
		\begin{tabular}{lccccccc}
			\toprule
			Stage & $|K_i|$ & $\Delta_{i,\min}^{\mathrm{impl}}$ & $\Delta_i$ & $k_{\mathrm{P},i}$ & $k_{\mathrm{I},i}$ & S/L/R & Timeout (s)\\
			\midrule
			PGA & 1 & 1 & 18 & $10^{-3}$ & $3\times10^{-4}$ & 512/512/1024 & 11.5\\
			BP  & 1 & 1 & 3 & $10^{-4}$ & $5\times10^{-4}$ & 512/1024/1024 & 17.3\\
			SUM & 7.89 & 8 & 10 & $10^{-4}$ & $5\times10^{-4}$ & 512/512/1024 & 11.5\\
			LP  & 6 & 6 & 8 & $10^{-4}$ & $5\times10^{-4}$ & 1024/1024/2048 & 17.3\\
			\bottomrule
		\end{tabular}%
	}
\end{table}

The proof-of-concept parameter set in Table~\ref{tab:cal_params} was selected empirically in preliminary bench tests, fixed across all reported conditions, and not systematically optimized; Section~\ref{sec:pga-note} discusses $\Delta_{\mathrm{PGA}}=18$. Finer trim can use a current-output DAC/resistor or buffered topology \cite{InfineonAN60305} without changing the sequence.
